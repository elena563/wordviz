version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"
  jobs:
    post_install:
      - pip install poetry
      - poetry install --with docs

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py
